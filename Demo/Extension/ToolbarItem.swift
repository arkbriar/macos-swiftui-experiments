//
//  ToolbarItem.swift
//  Demo
//
//  Created by shunjie.dsj on 2022/1/27.
//

import SwiftUI
import AppKit

struct ToolbarItems {
    public static let cloudSharing = ToolbarItem(id: NSToolbarItem.Identifier.cloudSharing.rawValue) {
        Text("")
    }

    public static let space = ToolbarItem(id: NSToolbarItem.Identifier.space.rawValue) {
        Text("")
    }

    public static let flexibleSpace = ToolbarItem(id: NSToolbarItem.Identifier.flexibleSpace.rawValue) {
        Text("")
    }

    public static let print = ToolbarItem(id: NSToolbarItem.Identifier.print.rawValue) {
        Text("")
    }

    public static let showFonts = ToolbarItem(id: NSToolbarItem.Identifier.showFonts.rawValue) {
        Text("")
    }

    public static let toggleSidebar = ToolbarItem(id: NSToolbarItem.Identifier.toggleSidebar.rawValue) {
        Text("")
    }

    public static let sidebarTrackingSeparator = ToolbarItem(id: NSToolbarItem.Identifier.sidebarTrackingSeparator.rawValue) {
        Text("")
    }
}
